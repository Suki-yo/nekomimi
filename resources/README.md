Packaged runtime resources for Nekomimi live here.

This directory is shipped via `electron-builder` `extraResources` so packaged
builds can stand up directly without depending on files outside the app bundle.
